import { Link } from "react-router-dom";

const SearchCard = ({ donationCard }) => {
    
    
      return (
          <div >
              
              
        </div>
      );
};

export default SearchCard;